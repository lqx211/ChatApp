# Chat record encryption

## How?
For user safety, we can encrypt user data with AES256.

## Warn
I backup everything, database and website root to /Volumes/Server/ca-db-bkup-260804181640.sql and /Volumes/Server/ca-fullbkup-260804181539.
Don't touch them!
DATABASE NAME: chatapp
use root, no password localhost login.

## Encryption area
Encrypt string messages from all users and groups.

## Database
Store AES256 private key and public key in user database.
Create a new row, `enckey`

**And check, if user's AES256 key is EMPTY, go to an assistant after log in:**
> IMMEDIATELY CLEAR AES256
> User saw a setup screen.
> User saws that,

```txt
From now on your new messages will be secured by encrypting it in our server.
Please enter your password again to generate a key.
[Field password]

[Continue]
```

## 打算
我打算是，用户传送的每个文本消息都会被AES256加密，就是保证